global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "application-service"
    metrics_path: "/actuator/prometheus"
    static_configs:
      - targets:
          - "application-service:8080"