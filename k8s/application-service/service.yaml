apiVersion: v1
kind: Service
metadata:
  name: application-service
  namespace: sre-lab
  labels:
    app: application-service
    component: backend
spec:
  type: ClusterIP
  selector:
    app: application-service
    component: backend
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: http