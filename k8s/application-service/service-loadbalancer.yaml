apiVersion: v1
kind: Service
metadata:
  name: application-service-lb
  namespace: sre-lab
  labels:
    app: application-service
spec:
  type: LoadBalancer
  selector:
    app: application-service
  ports:
    - name: http
      port: 8080
      targetPort: http