apiVersion: v1
kind: Service
metadata:
  name: application-service-nodeport
  namespace: sre-lab
  labels:
    app: application-service
spec:
  type: NodePort
  selector:
    app: application-service
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30080