apiVersion: v1
kind: Service
metadata:  
  name: alertmanager
  namespace: monitoring
  labels:
    app: alertmanager
    component: observability
spec:
  type: ClusterIP
  selector:
    app: alertmanager
  ports:
    - name: web
      port: 9093
      targetPort: web