spring:
  application:
    name: application-service

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,loggers
  endpoint:
    health:
      show-details: always

info:
  app:
    name: application-service
    owner: sre-lab
    purpose: first local Spring Boot service
